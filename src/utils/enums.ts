export enum ImageSize {
  SMALL = 'SMALL',
  NORMAL = 'NORMAL',
  BIG = 'BIG',
}

export enum TextType {
  HEADER = 'header',
  TITLE = 'title',
  BODY = 'body',
  SMALL = 'small',
  ERROR = 'error',
}

export enum ImageType {
  AVATAR,
  ICON
}

export enum UIReducerType {
  HANDLE_MODAL,
  RESET_MODAL
}
